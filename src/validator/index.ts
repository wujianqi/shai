import Validator, {
  ValidatorInterface,
  ValidSettingOption,
  RuleFunction,
  ChainInterface
} from "./validator";

export { ValidatorInterface, ValidSettingOption, RuleFunction, ChainInterface };
export default Validator;
